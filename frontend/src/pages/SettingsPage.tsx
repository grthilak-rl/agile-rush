import { EmptyState } from '../components/ui/EmptyState';

export default function SettingsPage() {
  return (
    <EmptyState
      icon="⚙️"
      title="Project Settings coming in Phase 4"
      description="Configure project details, sprint defaults, board columns, and more. Check back soon!"
    />
  );
}
