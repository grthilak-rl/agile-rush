# Database
POSTGRES_USER=agilerush
POSTGRES_PASSWORD=agilerush_secret
POSTGRES_DB=agilerush
DATABASE_URL=postgresql://agilerush:agilerush_secret@db:5432/agilerush

# JWT
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# Frontend
VITE_API_URL=http://localhost:8000/api

# Email (console = log to stdout, smtp = send real emails)
EMAIL_PROVIDER=console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@agilerush.com

# App
APP_URL=http://localhost:5173
